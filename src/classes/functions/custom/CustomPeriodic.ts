import { PeriodicFunction, PeriodicFunctionProps } from "../preset/PeriodicFunction";

class CustomPeriodicFunction extends PeriodicFunction {
    constructor(config: PeriodicFunctionProps) {
        super(config);
    }
}

export default CustomPeriodicFunction;